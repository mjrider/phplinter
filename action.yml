---
name: 'php-linter'
description: 'php-linter by pipeline components'
author: 'Robbert Müller <spam.me@grols.ch>'
branding:
  icon: 'play-circle'
  color: 'green'
inputs:
  directory:
    description: 'Directory to lint'
    required: false
    default: .
  options:
    description: 'Additional options'
    required: false
    default: ''
runs:
  using: 'docker'
  image: docker://registry.gitlab.com/pipeline-components/php-linter/amd64:07dbec67b37994424f902b17b91193ffc9957fb8
  args:
    - parallel-lint
    - '${{ inputs.options }}'
    - '${{ inputs.directory }}'
